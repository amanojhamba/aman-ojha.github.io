# Aman Ojha — Portfolio (GitHub Pages)

This is a static, recruiter‑ready portfolio site.

## 🚀 Quick Publish (GitHub Pages)

1. Create a public repository named **`aman-ojha.github.io`** on your GitHub account.
2. Upload all files in this folder to that repo (drag‑and‑drop on GitHub works).
3. Go to **Settings → Pages** and set **Branch: `main` / root**.  
4. Your site will be live at **https://aman-ojha.github.io**

## ✍️ Customize

- Replace `files/Aman_Ojha_Resume.pdf` with your actual resume (keep the same file name or update the link in `index.html`).
- Put your real images in `/images/` and update the `<img>` `src` values.
- Update text (headings, bullets, metrics) directly in `index.html`.
- The contact form uses Formspree; replace `action` with your own Formspree endpoint.

## 🧩 Structure

```
/
├── index.html
├── README.md
├── assets/
│   ├── css/style.css
│   └── js/main.js
├── images/           # posters, thumbnails, certificates
└── files/            # resume and downloadable files
```

## 🔒 Tips

- Redact certificate IDs if needed before uploading.
- Export images as WebP or optimized JPG to keep the site fast.
- Add a custom domain later via **Settings → Pages → Custom domain**.
